
/*
interface Person{
    firstName:string,
    lastName:string
}


function greeter(person: Person) {
    return "Hello, " + person.firstName + " " + person.lastName;
}


let user = { firstName: "Jane", lastName: "User" };

document.body.textContent = greeter(user);


*/

/*//  basic type checking

let x: [string, number] = tuple=array with a fixed number of 
elements whose types are known, but need not be the same porder
x = [10, "hello"]; // Error x=['hello',10] =ok
:void
:any
:generics =specifies type of constraint . / good for re-usable code



*/